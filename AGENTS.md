# 3구 당구 시뮬레이터 (3Pool)

웹(Three.js)에서 만드는 3구 캐럼 당구 시뮬레이터. 포켓 없는 대, 공 3개(흰·노랑 큐볼 + 빨강).
목표는 **실제에 가까운 물리** — 특히 큐 타격점(영어)이 정확히 반영되는 것.

## 현재 상태

**구현 완료**: 물리 코어 · 렌더 씬 · 입력/UI · 예측선 전부. `npm test` 9개 전부 통과, `npm run build` 통과.

```powershell
npm install        # 시작
npm test           # 물리 검증 (vitest)
npm run dev        # 개발 서버
npm run build      # tsc --noEmit + vite build
```

## 확정된 결정 (다시 논의하지 말 것)

| 결정 | 내용 | 이유 |
|---|---|---|
| 물리 엔진 | 범용(cannon-es/Rapier) 기각, **당구 특화 커스텀 물리 직접 구현** | 범용 엔진은 스핀·타격점·쿠션 반사가 대략적. "실제와 최대한 비슷"이 최우선 |
| 스택 | Vite + TypeScript + Three.js | web + 3D 요구 |
| 스코프 | **시뮬레이터 먼저** — 득점·턴 등 룰 없음 | 물리 검증에 집중, 게임화는 다음 단계 |
| 좌표계 | y-up, 테이블 평면 xz | Three.js 규약 |
| 조작 배치 (B안) | 마우스 좌우 이동=조준(상대 방식, 커서 위치 무관) · ←→ 각도(Shift=미세) · ↑↓ 파워 · 우클릭 드래그=타격점 · 휠=줌 · Space 발사 | 조준이 커서 좌표와 분리되어 HUD 조작 후에도 각도 유지, 요동 없음 |

## 물리 설계 사양 (분석 완료 — 그대로 구현)

### 규격 (캐럼 표준)
- 코스 간 2.84 × 1.42 m · 공 반경 R=0.0305 m(61 mm) · 공 질량 0.21 kg · 큐 유효질량 0.55 kg

### 타격 모델 (타격점 → 속도 + 스핀)
- 오프셋 a=수직, b=수평(물리값). UI 패드는 **±0.45R 로 클램프**(미스큐 영역 제외)
- 효율 η = 1/(1 + m/M + 5(a²+b²)/(2R²)), 공 속도 v = η·V_cue
- 각속도 ω = (b·ŷ − a·ŝ)·5v/(2R²), ŝ = d̂×ŷ(진행 방향의 오른쪽)
- 검산: a=+0.4R → 자연 롤링 성립 ✓, 중앙 타격 효율 ≈ 0.72(실제 ~70%) ✓
- b>0(오른쪽 타격) → ω_y>0 = 라이트 잉글리시

### 마찰 3단계
- **슬라이딩**(접촉점 미끄러짐 u = v + ω×(−Rŷ), |u|>ε): μs=0.2, u̇=−(7/2)μg·û, ω̇=(5μg/(2R))(ŷ×û)
- **롤링**: μr=0.01, 수평 ω 를 굴림 값(v/R)으로 스냅
- **수직축 스핀 감쇠**: μsp=0.044, 각감속 = 5μsp·g/(2R)

### 충돌
- **볼-볼**: e=0.95, 등질량 법선 교환 + 접선 마찰 throw — jt = min(μBB·jn, m|st|/7), μBB=0.06 — + 침투 위치 보정
- **쿠션**: 노즈 높이 1.25R(중심 위 0.25R). **수직 바운스 무시(평면 운동 한정)**.
  법선 반발 e=0.85, 접촉 높이 토크(r_c×Jn) + 마찰(m_eff = 1/(1/m + (|r_c|²−(r_c·t̂)²)/I), μ=0.2)
  → 사이드 스핀에 의한 반사각 변화가 자연히 나옴
- **검출**: 해석적 TOI 이벤트 주도 + 서브스텝 상한 2 ms. 터널링 없음.
  **결정론 필수** — RNG 사용 금지, 같은 입력 → 같은 결과

### 궤적 예측
별도 근사식이 아니라 **World.clone() 후 실제 시뮬을 사전실행**(16 ms 샘플링, horizon ~1.8 s).
예측 정확도가 시뮬과 동일해짐. 첫 충돌 위치 = 고스트 볼.

## 목표 파일 구조

```
src/
  physics/   constants.ts · ball.ts · collision.ts · world.ts · strike.ts · predict.ts
             ↑ 순수 로직. three 의 Vector3 까지만 사용, DOM/렌더 참조 금지
  render/    scene.ts(렌더러·조명·env) · table.ts · entities.ts(공·큐스틱·고스트·예측선)
  ui/hud.ts  DOM 오버레이 전부(파워·패드·버튼·도움말)
  main.ts    조립 · 루프 · 상태머신(aim ↔ run)
tests/physics.spec.ts
```

## 남은 할 일 (전부 완료 — 참고용 기록)

1. ~~`npm install`~~
2. ~~물리 코어 구현(위 사양)~~ — `src/physics/*`
3. ~~vitest 물리 테스트~~ — 9개 전부 통과 (머리맞대기/드로우/사이드 스핀 쿠션/결정론/에너지 단조 + 타격점 검산)
4. ~~렌더 씬~~ — `src/render/*`
5. ~~입력/UI~~ — `src/ui/hud.ts`, `src/main.ts`
6. ~~예측선 렌더~~ — cue dashed / 오브젝트 solid / 고스트 볼
7. ~~`npm test` + `npm run build` 통과~~

### 다음 단계 후보 (게임화)
- 득점·턴·파울 등 3구 룰
- 사운드, 상태 저장/복원

## 초기 배치

빨강 (0, +H/4) · 흰 (−0.19, −H/4) · 노랑 (+0.19, −H/4)

## 알려진 한계 (의도된 것)

- 평면 운동만 지원(공이 뜨지 않음) — 레벨 샷 기준 현실적. 마세·점프 샷 미지원
- 룰·소리·저장 없음(시뮬레이터 스코프). 게임 룰은 다음 단계
